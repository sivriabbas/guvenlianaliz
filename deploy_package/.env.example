# FASTAPI PRODUCTION ENVIRONMENT

# API Keys
API_KEY=your_api_football_key_here

# Environment
ENVIRONMENT=production
DEBUG=False

# Database (cPanel'den doldur)
DB_HOST=localhost
DB_NAME=username_analiz_db
DB_USER=username_dbuser
DB_PASSWORD=strong_password_here
DB_PORT=3306

# Security
SECRET_KEY=generate_random_secret_key
ALLOWED_HOSTS=xn--gvenlinaliz-dlb.com

# Redis (Shared hosting'de disable)
REDIS_ENABLED=False
