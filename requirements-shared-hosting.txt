# Namecheap Shared Hosting Requirements
# Optimized for shared hosting environments with resource limits

# ==========================================
# CORE WEB FRAMEWORK
# ==========================================
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6

# ==========================================
# DATABASE & ORM
# ==========================================
# MySQL support for shared hosting
mysqlclient==2.2.0
PyMySQL==1.1.0
sqlalchemy==2.0.23

# ==========================================
# LIGHTWEIGHT ML LIBRARIES
# ==========================================
# Core ML (required)
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.2

# Lightweight models (recommended for shared hosting)
lightgbm==4.1.0
xgboost==2.0.2

# Feature engineering
category-encoders==2.6.3

# ==========================================
# OPTIMIZATION & AUTOML
# ==========================================
optuna==3.4.0

# ==========================================
# API & UTILITIES
# ==========================================
requests==2.31.0
python-dotenv==1.0.0
pyyaml==6.0.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dateutil==2.8.2

# ==========================================
# LOGGING & MONITORING
# ==========================================
loguru==0.7.2
python-json-logger==2.0.7

# Optional: Sentry for error tracking
# sentry-sdk==1.38.0

# ==========================================
# DATA SCRAPING (if needed)
# ==========================================
beautifulsoup4==4.12.2
lxml==4.9.3

# ==========================================
# TESTING (optional - for development)
# ==========================================
# pytest==7.4.3
# pytest-asyncio==0.21.1
# httpx==0.25.2

# ==========================================
# NOTES FOR SHARED HOSTING
# ==========================================
# 1. TensorFlow/PyTorch EXCLUDED (too heavy for shared hosting)
# 2. Redis libraries excluded (shared hosting usually doesn't have Redis)
# 3. Use LightGBM instead of XGBoost when possible (faster)
# 4. Keep total installed size under 500MB
# 5. If installation fails, try: pip install --no-cache-dir -r requirements.txt
# 6. Some packages may need compilation - contact Namecheap support if errors occur

# ==========================================
# OPTIONAL HEAVY LIBRARIES (DISABLE FOR SHARED HOSTING)
# ==========================================
# tensorflow==2.15.0  # DON'T use on shared hosting
# torch==2.1.1        # DON'T use on shared hosting
# shap==0.43.0        # Heavy - use only if resources allow
# lime==0.2.0.1       # Heavy - use only if resources allow
